services:
  - type: cron
    name: run-focus
    schedule: "0 * * * *"  # 매시간 정각마다 실행 (UTC 기준)
    env: python
    buildCommand: "pip install -r /opt/render/project/src/requirements.txt"
    startCommand: "python focus.py"