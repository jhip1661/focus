  - type: cron
    name: run-marketing
    region: seoul           # ← 리전 설정 추가
    schedule: "0 * * * *"   # 매 시간 정각
    env: python
    buildCommand: "pip install -r /opt/render/project/src/requirements.txt"
    startCommand: "python marketing.py"

  - type: cron
    name: run-information
    region: seoul           # ← 리전 설정 추가
    schedule: "0 * * * *"   # 매 시간 정각
    env: python
    buildCommand: "pip install -r /opt/render/project/src/requirements.txt"
    startCommand: "python information.py"
